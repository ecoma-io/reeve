{
  "description": "The context engine's evidence gate, end to end: the diff adds `keeper()` and a `debugger;`, the rules' blocked list fires the deterministic preflight finding, and the review-stage prompt carries workspace evidence the fixture planted — the related-test path `src/app.test.ts` and the base-branch surrounding source line `old line`, neither of which the diff ever showed. The runner asserts that evidence surface separately from the finding ladder, so disabling the engine fails this fixture even though the blocked finding would still post.",
  "warrant": "version: 1\nlanguages:\n  - en\nduties:\n  review: [comment]\n",
  "files": [
    {
      "filename": "src/app.ts",
      "status": "modified",
      "additions": 2,
      "deletions": 0,
      "patch": "@@ -1,0 +1,2 @@\n+export function keeper(): void {\n+  debugger;\n+}"
    }
  ],
  "workspace": {
    "src/app.ts": "// base-branch source lives here, untouched by this diff\nold line\n",
    "src/app.test.ts": "import { keeper } from './app';\nit('keeps', () => { keeper(); });\n"
  },
  "rules": "version: 1\nblocked:\n  - phrase: debugger;\n    severity: warning\n    note: Do not commit debugger statements.\n",
  "verdict-over": {
    "findings": [],
    "confidence": 0.9
  },
  "expected": {
    "commented": "true",
    "findings": "1",
    "head-sha": "abc123",
    "workspace-evidence": "src/app.test.ts"
  }
}
